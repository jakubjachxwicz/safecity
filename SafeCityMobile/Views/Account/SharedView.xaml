<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SafeCityMobile.Views.Account.SharedView">

    <Grid BackgroundColor="{StaticResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition />
            <RowDefinition Height="40" />
            <!--<RowDefinition Height="40" />-->
        </Grid.RowDefinitions>

        <Label Text="{Binding PageTitle}"
               HorizontalTextAlignment="Center"
               VerticalTextAlignment="Center"
               TextColor="{StaticResource White}"
               FontAttributes="Bold"
               FontSize="18"
               BackgroundColor="{StaticResource TitleBackground}"/>

        <ContentView x:Name="DynamicContent"
                     Grid.Row="1"
                     Margin="8"/>

        <!--<Label Grid.Row="5"
               x:Name="InfoLabel"
               Text="{Binding InfoText}"
               FontSize="14"
               TextColor="Crimson"
               HorizontalTextAlignment="Center"
               VerticalTextAlignment="Start"/>-->

        <Label Grid.Row="2"
               Text="Polityka Prywatności i Regulamin"
               FontSize="14"
               TextColor="{StaticResource BlueishGray}"
               HorizontalTextAlignment="Center"
               VerticalTextAlignment="Start"/>
    </Grid>
</ContentView>
